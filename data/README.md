Place your datasets in this folder.

The starter notebook loads `sample.csv` by default so the template works immediately.
You can replace that file or add more files such as `.csv`, `.xlsx`, `.json`, or `.parquet`.
